const express = require('express');
const router = express.Router();
const paymentController = require('../controllers/stripe.controller.ts')

/*
TODO: write routes here to connect with client side
*/

module.exports = router;
